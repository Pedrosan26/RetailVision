// Placeholder for per-zone views -- built out in a later ticket, once
// zone configuration exists upstream. Routed and reachable now so the
// shell's nav is complete.

/** Renders a placeholder for the upcoming per-zone view page. */
export function ZonesPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-slate-900 dark:text-slate-100">Zones</h1>
      <p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Coming soon.</p>
    </div>
  );
}
