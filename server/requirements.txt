fastapi
uvicorn[standard]
sqlalchemy[asyncio]
asyncpg
alembic
pydantic-settings
pytest
pytest-asyncio
httpx
# aiosqlite: test-only -- backs an in-memory SQLite DB for endpoint tests
# so they don't require a running Postgres/TimescaleDB instance. Real
# deployment always uses Postgres via asyncpg; SQLite here validates the
# app/ORM logic, not TimescaleDB-specific behavior (that's exercised by
# actually applying the Alembic migration against real TimescaleDB).
aiosqlite

# numpy: the appearance comparison in dedup.py is a cosine over 512-dimensional
# vectors, run for every pair of tracks with no shared moment -- which is most
# pairs. In pure Python that loop dominated the whole request; here it is the
# one place the server does numerical work, and it is worth the dependency.
numpy
