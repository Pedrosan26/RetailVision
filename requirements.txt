opencv-python==4.10.0.84
torch
torchvision
ultralytics
Pillow
scikit-learn
pandas
# scipy: only for scipy.io.loadmat (scripts/widerface_baseline/official_eval.py
# reads WIDER FACE's official eval_tools ground-truth .mat files). Already
# pulled in transitively by scikit-learn, but listed explicitly since our own
# code imports it directly.
scipy
# requests: HTTP client used by remote_log.py to ship anonymized records to
# a central server when running multiple camera nodes.
requests
# deepface + tf-keras: the emotion model's DeepFace fallback comparison
# (scripts/evaluate_emotion_deepface.py) only, not used in the production
# pipeline — pulls in TensorFlow as a backend. tf-keras must be listed
# explicitly: deepface's Keras-3-era release doesn't declare it as a
# dependency even though its retinaface sub-dependency requires it.
deepface
tf-keras
